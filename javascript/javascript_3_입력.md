# javascript

## 입력

### 문자열을 입력하는 방법

#### 숫자를 입력 받는 방법

```php+HTML
<script>
let inputNumber = prompt('숫자를 입력하세요');	// 사용자 입력을 받는 코드
console.log(inputNumber);				// 입력받은 값을 콘솔에 출력
alert(inputNumber)					// 입력받은 값을 사용자에게 알림
</script>
```

![image-20200123105824574](images/image-20200123105824574.png)

![image-20200123105552320](images/image-20200123105552320.png)

![image-20200123110230845](images/image-20200123110230845.png)

=>console.log(변수 이름)을 이용시 변수(yn2, yn3)에 대해 확인을 클릭했다면 true, 취소를 클릭했다면 false를 반환해준다

=> 이 때 만약 같은 변수에 대해 let(선언)을 두번 이상 해준다면 오류가 날 수 있다. 선언시 변수에 저장되는 값은 변경될 수 없기 때문

![image-20200123111705194](images/image-20200123111705194.png)

=> 즉, 선언을 두번해주는 것이아닌 변수의 값만을 변경해주어야 한다.



#### 사칙연산

* 덧셈

<img src="images/image-20200123112250954.png" alt="image-20200123112250954" style="zoom:50%;" />

**=> 숫자와 문자열 덧셈을 제외한 사칙 연산은 숫자가 우선**

=> 숫자와 문자열 덧셈 연산은 문자열이 우선이다. 즉, 문자열 + 숫자 = 문자열 + 문자열



* 곱셈

<img src="images/image-20200123112659293.png" alt="image-20200123112659293" style="zoom:50%;" />

**=> 숫자와 문자열 덧셈을 제외한 사칙 연산은 숫자가 우선**

=> 숫자와 문자열 곱셈 연산은 숫자가 우선이다. 즉, 문자열 * 숫자 = 숫자 * 숫자



* NaN = Not a Number 

<img src="images/pasted%20image%200-1579746800527.png" alt="img" style="zoom:67%;" />

=> 자료형은 숫자이나 자바스크립로 나타낼 수 없는 숫자를 의미

=> 예) 자바스크립트에서는 복소수 표현이 불가능 



#### 불 자료형

: 다른 자료형을 불(bool: 참, 거짓을 표현하는 자료형)자료형으로 변환

: 0, NaN, '', null, undefined는 false로 변환하고, 나머지는  true로 변환

<img src="images/image-20200123114003773.png" alt="image-20200123114003773" style="zoom: 67%;" />

=> `0`, `NaN`, `' '`, `null`, `undefined`, `false` 를 제외한 모든 값은 true



#### 형변환

* 명시적 형변환

<img src="images/pasted%20image%200.png" alt="img" style="zoom: 67%;" />

**=> 명시적 형변환 = 강제적으로 형변환 (반대: 암시적 형변환)**

=> 다른 데이터 타입을 숫자형으로 변환 ⇒ Number() 

=> 함수를 사용다른 데이터 타입을 문자열로 변환 ⇒ String() 함수를 사용



* 암시적 형변환

<img src="images/pasted%20image%200-1579747473702.png" alt="img" style="zoom:80%;" />

=> 혼돈을 야기하는 문제점이 있음

ex)  

```php+HTML
<script>
	console.log('' == false);
	console.log('' == 0);
	console.log(0 == false);
	console.log('273' == 273);
</script> 
```

=> 이를 해결해주기 위한 방안으로 나온 것이 일치 연산자



#### 일치 연산자 (p.65)

: 양변의 자료형과 값의 일치 여부를 확인

* === : 양변의 자료형과 값이 모두 일치함
* !== : 양변의 자료형과 값이 일치하지 않음

```php+HTML
<script>
	console.log('' === false);
	console.log('' === 0);
	console.log(0 === false);
	console.log('273' === 273);
</script> 
	// 모두 false를 반환
```



#### 변수와 상수 구분

* 스코프(유효범위)

:특정 변수를 사용할 수 있는 유효 범위를 말한다. 일반적인 프로그래밍 언어에서는 특정 스코프 안에서 선언한 변수는 해당 스코프 안에서만 사용할 수 있지만 자바스크립트에서는 그렇지 않다. 그래서 `let`을  사용하면 이를 막을 수 있다.

![image-20200123131939572](images/image-20200123131939572.png)

​	=> 일반적인 프로그래밍 언어에서는 유효 범위가 각 중괄호까지이지만 자바스크립트에서는 var를 사용할경우 유효범위가 중괄호를 넘어간다.

* var키워드의 비동기 함수 문제

  * var사용

    ![image-20200123132712490](images/image-20200123132712490.png)

    ![image-20200123132824783](images/image-20200123132824783.png)

    => 출력 결과에서 앞의 동그라미는 똑같은 값이 반복되는 횟수를 나타냄

    => var는 변수 입력 값을 최상위로 보내기 때문에 같은 이름의 모든 변수가 같은 값을 갖게 됨 (거꾸로도 올라감)

    => setTimeout이라는 함수는 '`비동기함수`: 앞의 일을 끝내지 않고 다음일을 진행하는 방식'이다. 반대용어는 '`동기함수`: 미리 앞의 일을 끝내고 순차적으로 진행하는 방식'이다.

    => 출력값이 2 세 번이 아닌 3이 세 번인 이유는  setTimeout함수가 시간차를 두고 값을 출력하기 때문이다

    

  * let사용

    ![image-20200123133923965](images/image-20200123133923965.png)

    ![image-20200123133859644](images/image-20200123133859644.png)

​				=>  let을 통해 변수를 사용한다면 선언 이 후 부터 입력 값을 변화시켜가며 사용

​			**===> 결론은!!! var쓰지말고 let쓰자!!!!!**



